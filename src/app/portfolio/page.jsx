import { HeroSection } from "@/components/containers";

export const metadata = {
	title: "Homepage",
};

const Homepage1 = () => {
	return <HeroSection scroll={false} />;
};

export default Homepage1;
